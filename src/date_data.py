# Normalizar timestamps ISO (com ou sem Z) para ISO em UTC (+00:00).

# Calcular o número absoluto de dias entre dois timestamps ISO.

# Agrupar uma lista de timestamps por data (YYYY-MM-DD) e contar a frequência por dia.

# Verificar se um timestamp é de um fim de semana (sábado/domingo).

# Aplicar deslocamento de tempo (ex.: somar N horas) e retornar o novo ISO UTC.

# Converter uma lista de timestamps ISO para epoch seconds (UTC).

# Validar timestamps: retornar lista de válidos e de inválidos (erros de parsing).

# Detectar buracos de periodicidade: dada uma série diária, 
# listar datas faltantes entre min e max.