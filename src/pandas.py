# Pandas (básico e direto)

# Carregar vendas.csv em DataFrame.

# Converter valor para numérico (coerce), data_iso para datetime UTC.

# Filtrar valor > 0 e salvar em Parquet (data/processed/vendas_ok.parquet).

# Agregar por dia (YYYY-MM-DD) somando valor e salvar CSV.

# Fazer join com clientes.jsonl (ler como DataFrame) por cliente_id.

# Criar coluna categoria_valor (ex.: <=100, 100–300, >300) 
# e contar por categoria.

# Remover duplicatas por id, mantendo a primeira ocorrência.

# Validar schema: conferir presença e tipo de colunas esperadas; 
# reportar inconsistências.